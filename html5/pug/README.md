This section uses `pug` to compile html templates with various html elements to be used for demo content.

## Install/Use

- Install with `yarn`
- View templates in `./build` directory
- To recompile, run `yarn dev`